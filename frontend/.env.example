# 开发模式下 Vite proxy 转发的后端 API 地址
# 仅影响 npm run dev，构建产物不使用此变量
VITE_DEV_API_TARGET=http://127.0.0.1:8080

# 生产模式下前端请求的 API 基础地址
# 留空则使用当前页面的 origin（适用于前后端同域部署）
# 前后端分离部署时填写后端完整地址，如 https://api.example.com
VITE_API_BASE=
